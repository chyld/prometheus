$('#password_errors').empty();
$('#submit_password').attr('disabled', false);
$('#submit_password').val('Update');
$('#password').val('')

<% if @auth.errors.any? %>
  $('#password_errors').append('<%=j render partial: 'shared/errors', locals: {object: @auth} %>');
<% end %>
