$('#password-errors').empty();
$('#submit-password').attr('disabled', false);
$('#submit-password').val('Update Password');
$('#password').val('')
$('#password_confirmation').val('')

<% if @auth.errors.any? %>
  $('#password-errors').append('<%=j render partial: 'shared/errors', locals: {object: @auth} %>');
<% end %>
